# TODO
- [x] Update `vercel.json` to remove invalid `functions.runtime` configuration that causes Vercel build failure.
- [x] Remove `api/index.js` (conflicts with `api/index.ts`) and redeploy.
- [ ] If build passes but API/route still fails, adjust Vercel API/function entrypoint structure accordingly.



