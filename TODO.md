# TODO
- [ ] Update `vercel.json` to remove invalid `functions.runtime` configuration that causes Vercel build failure.
- [ ] Push redeploy and verify the build no longer fails with `Function Runtimes must have a valid version`.
- [ ] If build passes but API/route still fails, adjust Vercel API/function entrypoint structure accordingly.

